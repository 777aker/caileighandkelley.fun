function setup() {
  createCanvas(windowWidth, windowHeight);
  frameRate(30);
}

function draw() {
  // do the countdown stuff
  countdowndraw();


}

class Snake {
  constructor(x, y, len) {
    this.body = [];

  }
}
